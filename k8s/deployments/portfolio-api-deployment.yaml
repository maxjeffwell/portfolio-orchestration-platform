apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-api
  namespace: default
  labels:
    app: portfolio-api
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: portfolio-api
  template:
    metadata:
      labels:
        app: portfolio-api
    spec:
      serviceAccountName: portfolio-api-sa
      containers:
      - name: portfolio-api
        image: maxjeffwell/portfolio-api:20260202-020434-6092284
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
          name: http
          protocol: TCP
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        env:
        - name: PORT
          value: "5000"
        - name: NODE_ENV
          value: "production"
        - name: CORS_ORIGIN
          value: "https://pop-portfolio.el-jefe.me"
        - name: AUTH_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: portfolio-api-secrets
              key: auth-database-url
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: portfolio-api-secrets
              key: jwt-secret
        - name: ADMIN_USERNAME
          valueFrom:
            secretKeyRef:
              name: portfolio-api-secrets
              key: admin-username
        - name: ADMIN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: portfolio-api-secrets
              key: admin-password
        - name: PROMETHEUS_URL
          value: "http://prometheus-kube-prometheus-prometheus.monitoring:9090"
        - name: GOTIFY_URL
          value: "http://gotify.monitoring:80"
        - name: GOTIFY_APP_TOKEN
          valueFrom:
            secretKeyRef:
              name: portfolio-api-secrets
              key: gotify-app-token
        - name: GOTIFY_CLIENT_TOKEN
          valueFrom:
            secretKeyRef:
              name: portfolio-api-secrets
              key: gotify-client-token
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 15
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
