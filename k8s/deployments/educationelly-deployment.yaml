apiVersion: apps/v1
kind: Deployment
metadata:
  name: educationelly-client
  namespace: default
  labels:
    app: educationelly
    component: client
    tier: frontend
    portfolio: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: educationelly
      component: client
  template:
    metadata:
      labels:
        app: educationelly
        component: client
        tier: frontend
        portfolio: "true"
    spec:
      containers:
      - name: educationelly-client
        image: maxjeffwell/educationelly-client:20260113-073245-8237f5a
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
          name: http
          protocol: TCP
        resources:
          requests:
            memory: "64Mi"
            cpu: "10m"
          limits:
            memory: "256Mi"
            cpu: "250m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      restartPolicy: Always
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: educationelly-server
  namespace: default
  labels:
    app: educationelly
    component: server
    tier: backend
    portfolio: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: educationelly
      component: server
  template:
    metadata:
      labels:
        app: educationelly
        component: server
        tier: backend
        portfolio: "true"
    spec:
      containers:
      - name: educationelly-server
        image: maxjeffwell/educationelly-server:20260113-073245-8237f5a
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "8080"
        - name: ALLOWED_ORIGINS
          value: "https://educationelly-k8s.el-jefe.me,http://localhost:3000,http://localhost:3001"
        - name: MONGODB_URI
          valueFrom:
            secretKeyRef:
              name: educationelly-secret
              key: mongodb-uri
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: educationelly-secret
              key: jwt-secret
        resources:
          requests:
            memory: "64Mi"
            cpu: "10m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      restartPolicy: Always
