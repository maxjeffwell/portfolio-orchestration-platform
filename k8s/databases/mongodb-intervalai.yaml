apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-intervalai-pvc
  namespace: default
  labels:
    app: mongodb-intervalai
    tier: database
    protected: "true"
  annotations:
    helm.sh/resource-policy: keep
    description: "Protected PVC - manual deletion required"
  finalizers:
    - kubernetes.io/pvc-protection
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: v1
kind: Secret
metadata:
  name: mongodb-intervalai-secrets
  namespace: default
  labels:
    app: mongodb-intervalai
    protected: "true"
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
stringData:
  mongodb-root-password: "intervalai_mongo123"
  intervalai-db-password: "intervalai123"
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongodb-intervalai
  namespace: default
  labels:
    app: mongodb-intervalai
    tier: database
    protected: "true"
  annotations:
    helm.sh/resource-policy: keep
    description: "Protected database - requires manual deletion"
spec:
  serviceName: mongodb-intervalai
  replicas: 1
  selector:
    matchLabels:
      app: mongodb-intervalai
  template:
    metadata:
      labels:
        app: mongodb-intervalai
        tier: database
    spec:
      priorityClassName: portfolio-critical
      terminationGracePeriodSeconds: 60
      containers:
      - name: mongodb
        image: mongo:6
        ports:
        - containerPort: 27017
          name: mongodb
        env:
        - name: MONGO_INITDB_ROOT_USERNAME
          value: "root"
        - name: MONGO_INITDB_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mongodb-intervalai-secrets
              key: mongodb-root-password
        - name: MONGO_INITDB_DATABASE
          value: "intervalai"
        volumeMounts:
        - name: mongodb-storage
          mountPath: /data/db
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
        livenessProbe:
          exec:
            command:
              - mongosh
              - --eval
              - "db.adminCommand('ping')"
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 10
        readinessProbe:
          exec:
            command:
              - mongosh
              - --eval
              - "db.adminCommand('ping')"
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 10
      volumes:
      - name: mongodb-storage
        persistentVolumeClaim:
          claimName: mongodb-intervalai-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-intervalai
  namespace: default
  labels:
    app: mongodb-intervalai
spec:
  type: ClusterIP
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb
  selector:
    app: mongodb-intervalai
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-intervalai-nodeport
  namespace: default
  labels:
    app: mongodb-intervalai
  annotations:
    description: "NodePort for external access (testing/development)"
spec:
  type: NodePort
  ports:
  - port: 27017
    targetPort: 27017
    nodePort: 30027
    protocol: TCP
    name: mongodb
  selector:
    app: mongodb-intervalai
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: mongodb-intervalai-pdb
  namespace: default
  labels:
    app: mongodb-intervalai
    tier: database
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: mongodb-intervalai
