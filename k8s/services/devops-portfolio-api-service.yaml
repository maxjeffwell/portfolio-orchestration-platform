apiVersion: v1
kind: Service
metadata:
  name: devops-portfolio-api
  namespace: default
  labels:
    app: devops-portfolio-api
spec:
  type: ClusterIP
  selector:
    app: devops-portfolio-api
  ports:
  - name: http
    port: 5001
    targetPort: 5000
    protocol: TCP
