apiVersion: v1
kind: Service
metadata:
  name: portfolio-dashboard
  namespace: default
  labels:
    app: portfolio-dashboard
spec:
  type: NodePort
  selector:
    app: portfolio-dashboard
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
