apiVersion: v1
kind: Service
metadata:
  name: llama-cpu-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: llama-cpu
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
