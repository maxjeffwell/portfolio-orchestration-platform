apiVersion: v1
kind: Service
metadata:
  name: podrick-dashboard
  namespace: default
  labels:
    app: podrick-dashboard
spec:
  selector:
    app: podrick-dashboard
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
