apiVersion: v1
kind: Service
metadata:
  name: devops-portfolio-dashboard
  namespace: default
  labels:
    app: dashboard
spec:
  type: ClusterIP
  selector:
    app: dashboard
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  sessionAffinity: None
